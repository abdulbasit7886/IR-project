
# IR-project

IR-project is a prototype for image similarity search that uses CLIP to compute image embeddings and retrieve visually similar images. It consists of a Python backend for indexing and querying images and a Next.js frontend for searching and viewing results.
